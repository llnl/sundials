name: CI Containers - spack@develop

on:
  workflow_dispatch:
  schedule:
  - cron: '30 23 * * 0'

permissions:
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  build_ci_containers:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        indexsize: [32, 64]
        precision: ['double']
    steps:
    - name: VM Hardware Info
      run: cat /proc/cpuinfo

    - name: Checkout
      uses: actions/checkout@v6

    - name: Free up disk space
      uses: ./.github/actions/free-space-ubuntu

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4

    - name: Login to container registry
      uses: docker/login-action@v4
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker images
      uses: docker/build-push-action@v7.1.0
      with:
        context: "./scripts/docker"
        build-args: |
          SPACK_BASE_IMAGE=spack/ubuntu-noble:develop
          spack_yaml=int${{ matrix.indexsize }}-${{ matrix.precision }}/spack.yaml
        push: true
        tags: >-
          ${{ env.REGISTRY }}/llnl/sundials-ci-int${{ matrix.indexsize }}-${{ matrix.precision }}:spack-develop
        cache-from: type=gha,scope=sundials-ci-int${{ matrix.indexsize }}-${{ matrix.precision }}-develop
        cache-to: type=gha,mode=max,scope=sundials-ci-int${{ matrix.indexsize }}-${{ matrix.precision }}-develop
