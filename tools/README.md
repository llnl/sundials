# suntools (SUNDIALS)

This directory contains the `suntools` Python package, which provides CLI and a library interface for:

## Install

From the SUNDIALS repo root:

```bash
python -m pip install -e tools
```

Then import as:

```python
import suntools
```
